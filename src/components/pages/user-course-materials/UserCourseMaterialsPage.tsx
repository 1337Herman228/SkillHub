import CoursePassing from "@/components/course-passing/CoursePassing";

const UserCourseMaterialsPage = () => {
    return (
        <div>
            <CoursePassing role="user" />
        </div>
    );
};

export default UserCourseMaterialsPage;
